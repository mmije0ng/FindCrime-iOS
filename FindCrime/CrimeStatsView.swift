import SwiftUI

struct CrimeStatsView: View {
    var body: some View {
        VStack {
            Text("범죄 통계 화면")
                .font(.title)
                .padding()
            Spacer()
        }
    }
}
