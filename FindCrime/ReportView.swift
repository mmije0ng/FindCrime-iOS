import SwiftUI

struct ReportView: View {
    var body: some View {
        VStack {
            Text("제보 화면")
                .font(.title)
                .padding()
            Spacer()
        }
    }
}
