import SwiftUI

struct MyPageView: View {
    var body: some View {
        VStack {
            Text("마이페이지")
                .font(.title)
                .padding()
            Spacer()
        }
    }
}
