import SwiftUI

struct CrimeStatsView: View {
    var body: some View {
        VStack {
            Text("우리 지역 범죄 통계 화면")
                .font(.title)
                .padding()
            Spacer()
        }
    }
}
